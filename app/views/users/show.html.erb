<div class="container">
    <div class="row">
        <div class="col-xs-3">
            <%= render 'users/profile', user: @user, newbook: @newbook %>
        </div>
        <div class="col-xs-9">
            <h2>Books</h2>
            <table class="table">
            <thead>
                <th colspan=1></th>
                <th>Title</th>
                <th>Opinion</th>
            </thead>
            <% unless @books.blank? %>
                <% @books.each do |f| %>
                    <tr>
                        <td>
                            <%= link_to attachment_image_tag(f.user, :profile_image, :fill, size:'100x100', format: 'jpeg', fallback: "no_image.png"),user_path(f.user.id) %>
                        </td>
                        <td><%= link_to f.title, book_path(f.id) %></td>
                        <td><%= f.body %></td>
                    </tr>
                <% end %>
            <% end %>
            </table>
        </div>
    </div>
</div>